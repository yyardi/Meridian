# Handoff to the next manager agent — written 2026-09-13 (Sunday evening UTC)

Read this, then `STATUS.md`, then `docs/ARCHITECTURE.md`. Everything is on `main`.
There is one repo checkout (`~/Documents/Quant/Meridian`, on main, clean) and one
open agent worktree (dashboard, see §6). Do not create long-lived branches; the
operator wants one branch and one worktree. Small commits straight to main.

## 1. What the operator wants, in their words and mine
- Edge that pays, e.g. "$900/week", compounding; no "$15 to $300k" fantasies.
- **P&L is the scoreboard.** Every idea gets a paper line, scored weekly from venue
  settlements, and is judged on that table, not on my decompositions. Do not say
  "dead"; say "measured negative with power".
- Do not kill positive point estimates; register them, let the weeks accrue.
- Terse reports: did / result / meaning / blocked. Numbers in tables. No preamble.
- Nothing runs on the laptop overnight; automation lives on the AWS box (cron).
- Use subagents in parallel for building; consolidate code (target layout in
  ARCHITECTURE.md §4); keep the repo navigable.

## 2. Hard rules (never break)
- NEVER place, modify or cancel real orders. The executor is limit-only by
  construction; arming is the operator's script with the order token.
- Never print secrets (POLYMARKET_KEY_ID/SECRET_KEY, MERIDIAN_ORDER_TOKEN,
  MERIDIAN_NTFY_TOPIC, CFBD_API_KEY). Public repo: never commit IPs or AWS ids.
  The prod address is in `~/.meridian-server` (rotates); never write it anywhere.
- Prod is READ-ONLY for you except: `git checkout origin/main -- <files>` in
  /opt/meridian (allowed), crontab edits (allowed), writes to
  /opt/meridian/artifacts/reads (allowed), `cfb_game_map` writes via the map
  builder, and the `kalshi_games.game_start_time` data fix. `docker compose up`
  and `git commit` on prod are BLOCKED by the permission classifier: hand the
  operator the exact command instead.
- Analysis on prod: pipe scripts into the trainer image with `-e` env flags INSIDE
  `docker run` (an env prefix is dropped by sudo and the script silently runs its
  CFB default — check the first output line). One heavy script at a time. Every
  script sets `max_parallel_workers_per_gather=0` on connect and COMMITS it
  (64 MB /dev/shm cap). The paper book runs in the `meridian-api` container
  (`docker exec -i meridian-api python - < cfb/run_paper_book.py`) because it
  needs the venue client for settlements.
- Statistics: game-clustered sandwich, report mean [lo, hi] n G G_eff; G < 25 is
  UNDERPOWERED, never a result; pre-register reads by commit before the tape.
- On Polymarket US every slug is `<away>-<home>` and YES on every rung is the
  AWAY side. Any price-bucket finding must be split by side and checked against
  its home-referenced twin before it is believed (this killed two "excludes 0"
  results in one day). See memory `yes-is-always-the-away-team`.
- Timestamps in docs come from `date -u` (cross-checked against prod's clock) or
  git epoch, never from a sense of elapsed time.

## 3. What runs on the AWS box (all up as of this writing)
ESPN recorders (CFB, NFL), venue recorders (WNBA, CFB, NFL), live recorders,
DraftKings line recorders (NFL, CFB, 7 days ahead, change-detected), Kalshi
recorder (72h window; NFL games got kickoffs via a data fix on 09-12 — the
recorder's own path never gives NFL a window: durable fix needed in
`core/kalshi/recorder.py`), api on :8008, engines (quote, pulse, gridiron).
Cron (ubuntu user): Sun 15:50Z preflight, Mon 10:20Z gate → `scripts/prod_weekend_read.sh`
→ `/opt/meridian/artifacts/reads/<UTC>-<mode>.txt` (gate line + paper book + lag test).
Prod git is on `main-deploy` @ 41b6b70 with main's files STAGED (identical
content); the fast-forward is blocked by an untracked `cfb/run_making.py`.

## 4. Commands the operator still has to run (give them again if asked)
```
cd /opt/meridian && sudo docker compose -f docker-compose.yml -f docker-compose.mlb.yml up -d --build mlb-recorder
cd /opt/meridian && sudo mv cfb/run_making.py cfb/run_making.py.old && sudo git stash && sudo git merge --ff-only origin/main && sudo git stash pop
```
Verify MLB after: `docker ps | grep mlb`, then `market_snapshots` rows with slug `%-mlb-%`.

## 5. Results so far (details: STATUS.md, docs/RESEARCH_REPORT_2026-09-13.md)
- Market making: measured negative with power in every stratum (at plays −0.82¢,
  move side −1.17¢ after a look-ahead retraction, shadow maker −2.2% ROI on 169k
  fills). Don't reopen without new mechanism.
- Model vs price: never positive after the 6% fee.
- Paper book (first run 09-13, `docs/paper_book_2026-09-13.txt`): CFB 20–30¢ NO
  +$15.73 on $220 (107 games, +5.44¢ [−1.46, +12.35]); CFB home side every rung
  +$47 on $1,571 (+1.90¢ spans 0); WNBA under +$43.75 on $414 (spans 0; a
  late-August regime); WNBA favourites 80–100¢ +5.78¢ excludes 0 BUT its home
  twin −2.05¢ (away-listing artifact). MLB lines registered, no tape.
- Decompositions: CFB "longshot" effect is a ~4-pt whole-ladder home shift on
  90%-away-underdog weeks (or a 1.9σ draw); centre-rung away-cover 33/83 = 40%.

## 6. Open work
- Dashboard agent (branch `ui/honest-dashboard`, worktree under .claude/worktrees/):
  PULSE page WNBA-only wording + the stuck "1335" number, new SCOREBOARD page
  rendering the latest `paper_book*.txt` from `MERIDIAN_READS_DIR`. When it
  reports: review, `pytest --noconftest` its tests, merge to main, remove the
  worktree and branch, stage `core/api.py` + static on prod, and hand the operator
  `docker compose up -d --build api`.
- Registered reads: **Sat 09-19** (CFB 20–30¢ NO held-out + pooled; centre-rung
  home-shift by favourite side; Kalshi–DK lag with a full week; all in the Monday
  cron). WNBA playoffs: favourite/under lines, PULSE resumes. MLB: ladder
  calibration at 100 settled games (~a week after the recorder starts).
- Consolidation (ARCHITECTURE.md §4 migration order): step 2 is the strategies
  interface with the paper book as first client; the engines' `core.api` import;
  heartbeats for the odds/ESPN recorders; `scripts/health.py` blind to 5 containers.
- Durable fix: Kalshi recorder should set NFL `game_start_time` from ESPN on discovery.
- Operator's friend trades Kalshi NFL/CFB with a Monte Carlo model: the lag test
  is the mechanism check; still ask for venue / markets / fee / bet count.

## 7. Reporting to the operator
Lead with the number and its interval. If a read fails, say FAIL and what rule
it triggers. If something is blocked by permissions, give the exact command,
never a workaround. Update `STATUS.md` whenever state changes; it is the file
they open.
