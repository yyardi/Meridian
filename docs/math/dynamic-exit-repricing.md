# Dynamic exit re-pricing — registration

**Research agent, 2026-09-02. NOTHING COMPUTED FORWARD.** **Effective upon the
engine's shadow arm existing — builders build AGAINST this text**, not the other
way round.

## Arm

**Exit target recomputed each cycle at current FV** — exact rule pinned in the
arm's committed implementation before first read, **staleness-bounded per v3a's
fallback pattern** — versus the **STATIC incumbent**, paired forward, per-game
clustered.

## Motivation, quoted with its assumption named

The **+39.3¢/$ fill+1m counterfactual** assumes fills at the then-book —
**optimistic by construction, never evidence.**

**F8 caveat carried:** re-pricing fixes **staleness of the TARGET**, not
**adverse selection of the RESTING** order. A re-priced exit still rests against
flow ~37 seconds ahead.

## Gate

Paired per-$ CI excludes zero at floor.

**Floors:** ≥15 games with ≥1 diverging exit; ≥100 diverging exits.

**Closure:** 2× floors still straddling → **FAIL-BY-EXHAUSTION.**

## Re-netting clause — mandatory

**The verdict includes the re-netted premium/quintile table under the NEW exit
policy** (D's premium table as the instrument). **The compensation structure
moves with the exit rule**, so **no result from the incumbent-exit era may be
cited in this gate's interpretation without re-netting.**

---

**No in-sample result justifies capital. The forward test is the evidence.**

*Results append below this line, never above it.*
