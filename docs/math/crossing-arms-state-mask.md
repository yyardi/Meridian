# Crossing arms — state-mask companion registration

**Written by the research agent with Quant B, 2026-09-02. NOTHING COMPUTED
FORWARD.** Landed unmodified. Cohort cutoff is this document's own first commit,
read with
`TZ=UTC git log --format=%ct origin/main -- docs/math/crossing-arms-state-mask.md | tail -1`
and converted from epoch — never from prose.

**A COMPANION registration, never an edit to the landed gate.** The parent —
[intent-crossing-arms.md](intent-crossing-arms.md) — is untouched, its cohort
unaffected, its gate unchanged. **Landed gates do not grow arms** (#89-fg
precedent).

## Why this shape

Quant B's candidate 2 is **not registered as a standalone edge**, by its
finder's own accounting: a post-hoc filter, doubly-optimistic fills, chosen
among 197+ comparisons, with the pessimistic arm negative everywhere
**including the kept region**. But its **mechanism** is not weak — it is the
measured bookless-endgame / ride-tail fact (B×D joint note, #126/#127). So it
rides the crossing-arms vehicle rather than standing alone.

## Question

**Does masking entries in (Q4 ∪ |margin| ≥ 10) improve the cross-selective
arm's per-$?** This gates **the mask's marginal effect — one input, nothing
else.**

## Arm

**(b″)** = the parent registration's arm (b) eligibility **∧ NOT(Q4 ∪ |margin|
≥ 10)**.

The filter is pre-declared exactly as B stated it in the merged loss map:

> *entries decided outside (Q4 ∪ |margin| ≥ 10) outperform entries inside on
> per-$ outcome, forward cohort, clustered.*

## Score

Identical to the parent: `s·(S − touch_at_intent) − fee`, same tape, same
linking policy — **pinned to the `lineage_source`-REPAIRED rule**, with both
sides of the #147/#148 sensitivity quoted beside the pin (**+1.2¢/$ linked,
−10.0¢/$ unlinked**; 17 repaired links are 28% of that 60-row subset). Paired
per-game diff (b″ − b), **game-clustered**.

## Cohort

**Forward from this registration's landing instant.** The parent's cohort is
unaffected.

## Floors

**≥10 games in which the arms DIFFER** — a game where the mask removes nothing
is uninformative for the pair — **AND ≥100 parent-arm-(b) intents within them.**

## Gate

**PASS** = paired CI excludes zero in b″'s favour. **FAIL** = excludes zero
against.

**CLOSURE:** at 2× floors still straddling → **FAIL-BY-EXHAUSTION**, the mask is
not adopted, the gate closes.

## Disclosures — in the registration because they are the point

1. **The filter was chosen after seeing the ride composition.** This is its
   forward test. The in-sample **+2.4¢/$ [+0.5, +4.3]** (kept 1,235 fills / 31
   games) is quoted **only as motivation**, with its optimistic-fill caveat.
2. **#20's FAIL is inherited as CONTEXT, not evidence.** It removed one argument
   for Q4 entries — a possession-informed late edge — and **answers a different
   question than this gate.**
3. **A PASS here means THE MASK IMPROVES ARM (b).** It does **not** mean the
   kept region is profitable, and the write-up may never read it that way. **The
   pessimistic arm is negative everywhere** — stated now so it cannot be
   unstated later.

---

**No in-sample result justifies capital. The forward test is the evidence.**

*Results append below this line, never above it.*
