# Lever replay — design-not-evidence registration

**Research agent, 2026-09-02/03 (operator-ordered: "run back all the
previous games... get a ton of great understanding"). Landed by the
manager; this commit is the cutoff. A builds the variant harness, D
scores; both build to this text.**

**PURPOSE:** rank the v2 levers on the tape we own — the Aug pin
(`market_snapshots_quote_replay_20260902T173700Z.csv.gz`, md5
`b740d2fb6dcd5f325877cf8281a97c42`), 13 games, 17,032 ledgered fills — to
set ARM PRIORITY and expected magnitudes. DESIGN INPUTS, NEVER EVIDENCE:
forward gates keep their cutoffs and remain the only evidence; no output
here re-litigates any registered gate.

1. **Baseline known-answer (rule 16):** the replay harness reproduces v1's
   ledgered −1.60¢/fill before any variant runs. No reproduction, no wave.
2. **Levers and grids FROZEN NOW, before any variant runs:** PATIENCE (no
   requote into adverse micro-move, hold N ∈ {5s, 15s, 30s} — full grid
   reported, no cell selection); STATE/LATENESS suppression (cells
   pre-named from the landed loss-map artifacts only — no cell authored
   after seeing replay results); CONGESTION via the recorder proxy
   (descriptive only, the proxy's non-compliance label on every line — the
   pin predates the embargoed forward stream, but the label travels). Solo
   levers first, then pre-named compositions.
3. **Scoring:** D's markout machinery; capture basis with settlement
   printed; BOTH fill arms; game-clustered. Deliverable = the composition
   table: lever × arm × per-fill capture × total $.
4. **The multiple-comparisons haircut, stated not modeled:** ~3 levers ×
   grids × compositions means some cells flip −1.60 by luck; ranking uses
   effect size plus mechanism coherence across arms, never single-cell
   significance; and the winning composition's magnitude is reported WITH
   the selection-bias caveat in the same sentence — an in-sample-selected
   winner is upward-biased and the forward gate will read lower. Written
   where the operator reads it.
5. **The honesty line, once and plainly:** we cannot backtest games we did
   not record — no historical book feed exists for this venue. The tape we
   own is 13 August WNBA games plus everything recorded from tonight
   forward. The replay answers WHICH levers and ROUGHLY HOW MUCH; only the
   forward gates answer WHETHER.
6. **Output routing:** the ranking sets which arm's gate registers first
   and what magnitude its floors should anticipate — it never sets
   thresholds. Thresholds are born in the forward gates' own
   registrations, per amendment 4.

---

*Results append below this line, never above it.*
