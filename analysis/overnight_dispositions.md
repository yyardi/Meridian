# Overnight dispositions — 2026-09-02

Research agent's rulings on the three survivors of the overnight run, plus the
strategic statement the work forces.

## THE STRATEGIC STATEMENT — read this first

**The resting-maker book as constituted is NOT rescued by exit hygiene.**

Two measured facts force it:

- Under the measured 4.70¢ concession, the **relabelled ride share alone is
  17.7%** — above the **11.4% breakeven** on its own.
- Late-state exit premiums (**Q4 3.51¢/ct, min 5–10 3.83¢/ct**) **exceed the
  model's entire measured alpha of 3.47¢/ct.**

**Exit work makes losses smaller and sizing honest. It does not make the book
positive.** The live question for positivity remains **entry policy** — where
the crossing arms and the state mask are already pointed.

*"We fixed the rides" will otherwise be read as "the book works." It does not
follow and must not be written that way.*

## The deaths, endorsed

**"Remove the rides, keep the wins" — dead.** B's predictor works (LOGO AUC
0.700, calibrated) and its realized per-$ is **flat across risk quintiles**.
**The price already charges for detectable risk** — now demonstrated three
independent times in this project: boundary deviation (#18), ESPN win
probability (F9), and ride risk. **A working instrument was required to prove
the filter dead**, which is why it counts.

**The exit-rule fix — inconclusive, and landed as a guard.** The counterfactual
inverts every naive sign; the game-clustered intervals span zero at 32 games.
Its value is stopping a coin-flip loosening of the profit target and protecting
`fv_adverse` from removal. **The right use of a null.**

## Disposition 1 — P(ride) as a variance input to Kelly: REGISTER

**The surviving class: it needs no edge and beats no market.** It is correct
risk accounting for an engine that currently sizes a 30%-tail and a 2%-tail
entry identically.

Arm = fractional Kelly with a per-entry left tail from P(ride), versus the
incumbent flat fraction, scored forward on the shadow tape **at equal mean
exposure**; metric = realized log-growth and max-drawdown **paired per game,
clustered**; floors in games; closure clause. Model form pinned by B in a
committed file **pre-read**.

**MANDATORY PRINTED CHECK — B's own pre-committed null: `q5 − q1 mean ≈ 0`
in-cohort.** If the variance arm's gain correlates with mean-selection, **it
rebuilt a filter and the verdict must say so.** *That null being pre-committed
by the model's own author is why this can be trusted — preserve it verbatim.*

## Disposition 2 — dynamic exit re-pricing: APPROVED AS CANDIDATE

Registration written when the engine can express the arm (builder dependency: a
shadow exit-repricing arm in pulse). Constraints fixed now:

- The **exact re-pricing rule pinned pre-read** — recompute the exit at the FV
  target each cycle, staleness-bounded.
- The **+39.3¢/$ counterfactual is MOTIVATION ONLY**, with its fill assumption
  named: an early exit that *"would have saved"* assumes a fill at the then-book
  — **optimistic by construction**.
- **The F8 caveat carries:** a re-priced exit still rests against flow ~37
  seconds ahead. **Repricing fixes staleness of the TARGET, not adverse
  selection of the RESTING.**
- Paired against the static incumbent on the forward tape.

## Disposition 3 — δ*(p): BLOCKED

Blocked as **D themselves wrote it**, pending the B–D reconciling paragraph. The
question that paragraph must answer:

- **(i) REDUNDANT with p** — B's flat quintiles say the mean is already priced,
  so δ* as an *entry criterion* double-counts and dies; or
- **(ii) a COST MODEL** — an accounting of exit-leg friction belonging inside
  sizing/EV arithmetic (with disposition 1) rather than as an entry filter
  competing with the price.

**Research agent's prior is (ii).** If they land there, **δ* never needs to beat
the market — it needs to be measured**, which D has already done.

---

**No in-sample result justifies capital. The forward test is the evidence.**
